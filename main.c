#include "monty.h"

/**
 * main - entry point
 * @ac: number of arguments
 * @av: array of arguments
 *
 * Return: exit status
 */
int main(int ac, __attribute__((unused))char **av)
{
	if (ac != 2)
		file_err(NULL);
	return 1;
}
